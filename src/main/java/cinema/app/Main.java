package cinema.app;

import javafx.application.Application;

// Point d'entrée du programme : sans cette classe, l'appli JavaFX ne démarre pas.
// On lance juste MainApplication (c'est elle qui affiche les fenêtres).
public class Main {

    // Méthode main = la première chose que Java appelle quand on lance le .jar ou le projet.
    public static void main(String[] args) {
        Application.launch(args);
    }
}
